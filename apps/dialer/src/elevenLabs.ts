export async function synthesizeSpeech(_text: string): Promise<AsyncIterable<Uint8Array>> {
  // TODO: Stream low-latency TTS audio chunks
  async function* gen() {
    // placeholder async generator
  }
  return gen();
}
